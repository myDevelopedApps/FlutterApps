name: graph_section
description: "A new Flutter project."
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.6.0

dependencies:
  fl_chart: ^0.70.0
  flutter:
    sdk: flutter
  google_fonts: ^6.2.1
  syncfusion_flutter_charts: ^28.1.36

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
